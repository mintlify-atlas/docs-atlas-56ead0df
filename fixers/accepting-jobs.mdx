---
title: 'Accepting Jobs'
description: 'Create, manage, and optimize your job offers to attract more clients'
---

## Job Offers Overview

Job offers are your service listings on Servineo. They showcase what you offer, your pricing, and help clients find and contact you.

## Creating Job Offers

Access the Job Offers section from your dashboard:

```tsx
import { JobOffersSection } from '@/Components/fixer/dashboard/JobOffersSection';

<JobOffersSection readOnly={false} />
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:41`

## Job Offer Data Structure

```typescript
interface IJobOffer {
  _id: string;
  fixerId: string;
  fixerName: string;
  title: string;
  description: string;
  category: string;
  tags?: string[];
  price: number;
  city: string;
  contactPhone: string;
  photos: string[];
  rating?: number;
  status?: boolean;  // Active/Inactive
  createdAt?: string;
  updatedAt?: string;
}
```

See type definition: `src/types/fixer-profile.ts:1`

## Available Service Categories

Choose from predefined categories:

```typescript
export const jobCategories = [
  { value: 'Plomería', label: 'Plomería' },
  { value: 'Electricidad', label: 'Electricidad' },
  { value: 'Carpintería', label: 'Carpintería' },
  { value: 'Pintura', label: 'Pintura' },
  { value: 'Albañilería', label: 'Albañilería' },
  { value: 'Jardinería', label: 'Jardinería' },
  { value: 'Limpieza', label: 'Limpieza' },
  { value: 'Reparación de electrodomésticos', label: 'Reparación de electrodomésticos' },
  { value: 'Instalación de pisos', label: 'Instalación de pisos' },
  { value: 'Techado', label: 'Techado' },
];
```

## Creating a New Job Offer

### Form Validation

Use Zod schema for validation:

```typescript
import { zodResolver } from '@hookform/resolvers/zod';
import { jobOfferSchema } from '@/app/lib/validations/Job-offer-Schemas';

const { register, handleSubmit, formState: { errors } } = useForm({
  resolver: zodResolver(jobOfferSchema),
  defaultValues: {
    title: '',
    description: '',
    category: '',
    price: 0,
    city: 'Cochabamba',
    contactPhone: user?.telefono || '',
    tags: [],
  },
});
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:68`

### Form Fields

#### Title and Category

```tsx
{/* Title */}
<div>
  <label className='block text-sm font-medium text-gray-700 mb-1'>
    Título del servicio
  </label>
  <input
    {...register('title')}
    className='w-full rounded-lg border-primary border py-2 px-3'
    placeholder='Ej: Reparación de tuberías'
  />
  {errors.title && <p className='text-red-500 text-xs'>{errors.title.message}</p>}
</div>

{/* Category */}
<div>
  <label className='block text-sm font-medium text-gray-700 mb-1'>
    Categoría
  </label>
  <select {...register('category')} className='w-full rounded-lg border py-2 px-3'>
    <option value=''>Seleccionar categoría</option>
    {jobCategories.map((cat) => (
      <option key={cat.value} value={cat.value}>{cat.label}</option>
    ))}
  </select>
</div>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:391`

#### Tags Management

Add up to 5 tags to your offer:

```tsx
const currentTags = watch('tags') || [];

const handleAddTag = (e: React.ChangeEvent<HTMLSelectElement>) => {
  const value = e.target.value;
  if (!value) return;

  if (!currentTags.includes(value) && currentTags.length < 5) {
    setValue('tags', [...currentTags, value], { shouldValidate: true });
  }
  e.target.value = '';
};

const removeTag = (tagToRemove: string) => {
  const newTags = currentTags.filter((t) => t !== tagToRemove);
  setValue('tags', newTags, { shouldValidate: true });
};
```

```tsx
{/* Tag display */}
<div className='flex flex-wrap gap-2'>
  {currentTags.map((tag) => (
    <span key={tag} className='inline-flex items-center gap-1 px-3 py-1 rounded-full bg-blue-100'>
      {tag}
      <button onClick={() => removeTag(tag)}>
        <X size={12} />
      </button>
    </span>
  ))}
</div>

{/* Tag selector */}
<select onChange={handleAddTag} disabled={currentTags.length >= 5}>
  <option value=''>Agregar etiqueta</option>
  {jobCategories.map((cat) => (
    <option key={cat.value} value={cat.value} disabled={currentTags.includes(cat.value)}>
      {cat.label}
    </option>
  ))}
</select>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:99`

#### Description, Price, and Location

```tsx
{/* Description */}
<div>
  <label>Descripción</label>
  <textarea
    {...register('description')}
    rows={4}
    className='w-full rounded-lg border py-2 px-3'
    placeholder='Describe el servicio que ofreces...'
  />
</div>

{/* Price and City */}
<div className='grid grid-cols-2 gap-4'>
  <div>
    <label>Precio (Bs)</label>
    <input type='number' {...register('price')} className='w-full rounded-lg border py-2 px-3' />
  </div>
  <div>
    <label>Ciudad</label>
    <select {...register('city')} className='w-full rounded-lg border py-2 px-3'>
      {boliviaCities.map((city) => (
        <option key={city.value} value={city.value}>{city.label}</option>
      ))}
    </select>
  </div>
</div>

{/* Contact Phone */}
<div>
  <label>Teléfono de contacto</label>
  <input {...register('contactPhone')} className='w-full rounded-lg border py-2 px-3' />
</div>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:480`

#### Photo Upload

Upload up to 5 images:

```tsx
const [selectedImages, setSelectedImages] = useState<File[]>([]);
const [previewUrls, setPreviewUrls] = useState<string[]>([]);

const handleImageChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const files = Array.from(e.target.files || []);
  
  // Check total count
  if (selectedImages.length + files.length > 5) {
    showNotify('warning', 'Límite alcanzado', 'Máximo 5 imágenes');
    return;
  }
  
  // Check file size (max 5MB)
  const newFiles: File[] = [];
  files.forEach((file) => {
    if (file.size > 5 * 1024 * 1024) {
      showNotify('error', 'Imagen muy grande', `${file.name} excede 5MB`);
    } else {
      newFiles.push(file);
    }
  });
  
  const newPreviews = newFiles.map((file) => URL.createObjectURL(file));
  setSelectedImages((prev) => [...prev, ...newFiles]);
  setPreviewUrls((prev) => [...prev, ...newPreviews]);
};

const removeImage = (index: number) => {
  setSelectedImages((prev) => prev.filter((_, i) => i !== index));
  if (previewUrls[index].startsWith('blob:')) {
    URL.revokeObjectURL(previewUrls[index]);
  }
  setPreviewUrls((prev) => prev.filter((_, i) => i !== index));
};
```

```tsx
<div className='grid grid-cols-4 gap-2'>
  {previewUrls.map((url, idx) => (
    <div key={idx} className='relative aspect-square group'>
      <Image src={url} className='w-full h-full object-cover rounded-lg' alt='preview' />
      <button
        onClick={() => removeImage(idx)}
        className='absolute top-1 right-1 bg-red-500 text-white p-1 rounded-full opacity-0 group-hover:opacity-100'
      >
        <X size={14} />
      </button>
    </div>
  ))}
  
  {previewUrls.length < 5 && (
    <label className='flex flex-col items-center justify-center border-2 border-dashed rounded-lg aspect-square cursor-pointer'>
      <Upload className='text-gray-400' />
      <input type='file' className='hidden' accept='image/*' multiple onChange={handleImageChange} />
    </label>
  )}
</div>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:115`

### Submitting Job Offers

Use RTK Query mutation to create or update offers:

```typescript
import { useCreateJobMutation, useUpdateJobMutation } from '@/app/redux/services/jobApi';

const [createJob, { isLoading: isCreating }] = useCreateJobMutation();
const [updateJob, { isLoading: isUpdating }] = useUpdateJobMutation();

const onSubmit = async (data: JobOfferFormData) => {
  const formData = new FormData();
  formData.append('fixerId', user._id);
  formData.append('fixerName', user.name || 'Usuario');
  formData.append('title', data.title);
  formData.append('description', data.description);
  formData.append('category', data.category);
  formData.append('price', data.price.toString());
  formData.append('city', data.city);
  formData.append('contactPhone', data.contactPhone);
  formData.append('tags', JSON.stringify(data.tags));
  
  selectedImages.forEach((file) => formData.append('photos', file));
  
  if (editingOffer) {
    await updateJob({ jobId: editingOffer._id, formData }).unwrap();
    showNotify('success', 'Actualizado', 'Oferta actualizada exitosamente');
  } else {
    await createJob(formData).unwrap();
    showNotify('success', 'Publicado', 'Oferta creada exitosamente');
  }
  
  handleCloseModal();
};
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:182`

## Managing Job Offers

### Listing Job Offers

Fetch and display your offers:

```typescript
import { useGetJobsByFixerQuery } from '@/app/redux/services/jobApi';

const { data: apiOffers, isLoading, refetch } = useGetJobsByFixerQuery(userId, { 
  skip: !userId 
});
```

```tsx
<div className='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6'>
  {apiOffers?.map((offer) => (
    <JobOfferCard
      key={offer._id}
      offer={offer}
      onEdit={() => handleOpenModal(offer)}
      onDelete={() => confirmDelete(offer._id!)}
      onToggleActive={() => handleToggleActive(offer._id!)}
      onCreatePromo={() => handleCreatePromo(offer._id)}
    />
  ))}
</div>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:46`

### Filtering by Status

Filter offers by active/inactive status:

```tsx
const [statusFilter, setStatusFilter] = useState<'all' | 'active' | 'inactive'>('all');

const filteredOffers = apiOffers?.filter((offer) => {
  if (statusFilter === 'all') return true;
  const isActive = offer.status ?? true;
  return statusFilter === 'active' ? isActive : !isActive;
}) || [];

<select
  value={statusFilter}
  onChange={(e) => setStatusFilter(e.target.value as 'all' | 'active' | 'inactive')}
  className='rounded-full px-4 py-2'
>
  <option value='all'>Todas</option>
  <option value='active'>Activas</option>
  <option value='inactive'>Inactivas</option>
</select>
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:64`

### Toggling Offer Status

Activate or deactivate job offers:

```typescript
import { useToggleJobStatusMutation } from '@/app/redux/services/jobApi';

const [toggleJobStatus] = useToggleJobStatusMutation();

const handleToggleActive = async (jobId: string) => {
  try {
    await toggleJobStatus({ jobId }).unwrap();
    await refetch();
    showNotify('success', 'Estado cambiado', 'El estado de la oferta ha sido actualizado');
  } catch (err) {
    console.error('Error toggling status:', err);
    showNotify('error', 'Error', 'No se pudo cambiar el estado');
  }
};
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:252`

### Deleting Job Offers

Delete offers with confirmation:

```typescript
import { useDeleteJobMutation } from '@/app/redux/services/jobApi';

const [deleteJob] = useDeleteJobMutation();

const confirmDelete = (jobId: string) => {
  showNotify(
    'warning',
    '¿Eliminar oferta?',
    'Esta acción no se puede deshacer',
    async () => {
      try {
        await deleteJob({ jobId, fixerId: userId }).unwrap();
        showNotify('success', 'Eliminado', 'Oferta eliminada exitosamente');
      } catch (error) {
        showNotify('error', 'Error', 'No se pudo eliminar la oferta');
      }
    }
  );
};
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:218`

## Promotions

Create promotional campaigns for your job offers:

```tsx
import CreatePromoModal from '@/app/components/fixers/CreatePromoModal';
import { createPromotion } from '@/services/promotions';

const handleCreatePromo = async (promotion: {
  title: string;
  description: string;
  offerId: string;
  fixerId: string;
  price: string;
}) => {
  const result = await createPromotion({
    title: promotion.title,
    description: promotion.description,
    offerId: promotion.offerId,
    price: promotion.price,
    fixerId: promotion.fixerId,
  });

  if (result) {
    showNotify('success', 'Promoción creada', 'Tu promoción está activa');
    await refetch();
  }
};
```

Implementation: `src/Components/fixer/dashboard/JobOffersSection.tsx:265`

## Best Practices

<AccordionGroup>
  <Accordion title="Professional Photos">
    - Use high-quality, well-lit images
    - Show your actual work, not stock photos
    - Include before/after shots when possible
    - Keep images under 5MB each
  </Accordion>

  <Accordion title="Compelling Descriptions">
    - Be specific about what you offer
    - Highlight your expertise and experience
    - Mention any certifications or specializations
    - Keep it concise but informative
  </Accordion>

  <Accordion title="Competitive Pricing">
    - Research market rates in your area
    - Consider your experience level
    - Be transparent about pricing
    - Update prices based on demand
  </Accordion>

  <Accordion title="Effective Tags">
    - Use relevant service categories
    - Add specific skill tags
    - Include location-specific tags
    - Maximum 5 tags per offer
  </Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Calendar Management" icon="calendar" href="/fixers/calendar-management">
    Manage your availability
  </Card>
  <Card title="Building Reputation" icon="star" href="/fixers/building-reputation">
    Grow your ratings and reviews
  </Card>
</CardGroup>
